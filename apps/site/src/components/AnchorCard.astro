---
import type { EssayAnchor } from '../content/structural-essays/types';

interface Props {
  anchor: EssayAnchor;
}

const { anchor } = Astro.props as Props;
---

<article class="poc-anchor-card">
  <div class="poc-anchor-card-copy">
    <p class="poc-anchor-entry">{anchor.entryLabel}</p>
    <h3>{anchor.title}</h3>
    <p class="poc-anchor-signal">{anchor.signal}</p>
    <p class="poc-anchor-question">{anchor.question}</p>
  </div>
  <a class="poc-anchor-card-cta" href={anchor.href}>Start here</a>
</article>

<style>
  .poc-anchor-card {
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
    padding: 0.95rem 1rem;
    border: 0.5px solid color-mix(in srgb, var(--poc-box-card-border) 72%, var(--poc-box-border));
    border-inline-start: 0.08rem solid color-mix(in srgb, var(--poc-box-card-rail) 62%, var(--poc-border));
    border-radius: 0.8rem;
    background: var(--poc-box-row-surface);
    backdrop-filter: blur(var(--poc-blur-sm));
    box-shadow: none;
  }

  .poc-anchor-card > * {
    margin: 0;
  }

  .poc-anchor-card-copy {
    display: grid;
    gap: 0.58rem;
    align-content: start;
  }

  .poc-anchor-card-copy > * {
    margin: 0;
  }

  .poc-anchor-entry {
    color: var(--poc-text-soft, var(--sl-color-gray-3));
    font-size: 0.78rem;
    font-weight: 650;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .poc-anchor-card h3 {
    color: var(--poc-box-card-title);
    font-size: 1rem;
  }

  .poc-anchor-signal {
    color: var(--poc-text-soft, var(--sl-color-gray-3));
    font-size: 0.92rem;
    line-height: 1.5;
  }

  .poc-anchor-question {
    color: var(--poc-text-strong, var(--sl-color-gray-2));
    font-weight: 600;
  }

  .poc-anchor-card-cta {
    width: fit-content;
    align-self: flex-end;
    text-align: right;
  }
</style>
