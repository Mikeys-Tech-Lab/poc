---
title: "Sensible Defaults: A Lens You Can Load"
---

import ActivationPrompts from '../../../../../../../components/ActivationPrompts.astro';
import ShareButton from '../../../../../../../components/ShareButton.astro';

<ShareButton />

This is not another Act.

It is the first bridge from reading the practice to using it.

If your work feels confusing, overloaded, or harder to plan than it should, this lens helps make the hidden parts visible.

For example, if a plan names features but says nothing about data, infrastructure, or QA, this lens helps surface that missing work early.

This system only operates when its artifacts are loaded.

In practice, this means: paste the context seeder.

Without that, this is just description.

### What changes when the lens loads

- hidden work becomes easier to name
- missing delivery domains become easier to notice
- planning pressure becomes easier to inspect
- the next honest check becomes easier to ask for

This is not extra process. It is a way to reduce hidden load by making the work easier to see.

### Try it

Use the canonical lens package in the repository:

- [context-seeder.md](https://github.com/Mikeys-Tech-Lab/poc/blob/main/mandateLenses/SensibleDefaults/context-seeder.md)
- [lens.md](https://github.com/Mikeys-Tech-Lab/poc/blob/main/mandateLenses/SensibleDefaults/lens.md)

The article is the human bridge. The repository artifacts are what actually get loaded.

For live use, load the repository artifact or a local checkout of the same path.

If the system cannot load the source, it should not pretend it is using the lens.

### Paste-ready activation

<ActivationPrompts intro="If you already know this is the lens you want, paste one of these prompts into a compatible model or agent. The article is the bridge. The seeder is what actually loads the lens." />
